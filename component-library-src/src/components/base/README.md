# Base Components

Place the smallest reusable interface elements here. A base component owns a defined semantic, visual, content, or interaction contract and should not encode article- or page-specific layout.

Examples include buttons, badges, dividers, icons, and form controls.
